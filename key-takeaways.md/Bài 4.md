###### 1.Phạm vi của biến: xác định phạm vi mà biến có thể truy cập
 - Block scope (khối): biến được khai báo trong cặp ngoặc nhọn
     - var: không bị giới hạn bởi cặp ngoặc nhọn
     - let/const: bị giới hạn bởi cặp ngoặc nhọn, ra ngoài là bị undefine
- Function scope (hàm): biến được khai báo trong một hàm
     - Cả let/var/const: ra ngoài hàm là undefine
- Toàn cục : biến được khai báo ở một dòng code tự do không nằm trong khối hay hàm
###### 2. Break and continue
- Break dùng để thoát hoàn toàn khỏi vòng lặp ngay lập tức
- continue dùng để bỏ qua phần còn lại của vòng lặp hiện tại và chuyển sang lần lặp tiếp theo
###### 3. Câu điều kiện nâng cao
 - Câu điều kiền if...else
 - Câu điều kiện if...else...if
 